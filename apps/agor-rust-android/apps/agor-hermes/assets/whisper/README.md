Place `ggml-base.en.bin` here to package the on-device Whisper fallback model.

Run:

```bash
scripts/fetch-whisper-model.sh base.en
```

The file is intentionally not committed because the model is large.

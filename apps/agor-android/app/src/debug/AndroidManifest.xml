<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <application
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true"
        tools:replace="android:usesCleartextTraffic">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="live.agor.app.action.NATIVE_LOGIN" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <intent-filter>
                <action android:name="live.agor.app.action.NATIVE_HERMES_TRIGGER" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <intent-filter>
                <action android:name="live.agor.app.action.AGOR_CONTROL" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".automation.HermesControlReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="live.agor.app.action.AGOR_CONTROL" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
